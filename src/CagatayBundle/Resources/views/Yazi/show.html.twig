{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Yazi</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Baslik</th>
                <td>{{ entity.baslik }}</td>
            </tr>
            <tr>
                <th>Metin</th>
                <td>{{ entity.metin }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ entity.createdAt|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('yazi') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('yazi_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
